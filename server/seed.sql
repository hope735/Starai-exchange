-- Seed data: nothing required for now. Future versions can pre-populate
-- a list of supported coins, fee schedules, etc. Leaving the file empty
-- would cause mysql2 to error, so we ship a no-op SELECT.
SELECT 1;
